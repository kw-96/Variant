# 导入团队库组件功能 - 实现方案

## 📋 目录

- [一、功能概述](#一功能概述)
- [二、核心发现](#二核心发现)
- [三、功能设计](#三功能设计)
- [四、技术实现方案](#四技术实现方案)
- [五、UI 设计方案](#五ui-设计方案)
- [六、文件结构](#六文件结构)
- [七、实现优先级](#七实现优先级)
- [八、错误处理](#八错误处理)
- [九、用户操作指引](#九用户操作指引)
- [十、实现步骤](#十实现步骤)
- [十一、方案优势](#十一方案优势)

---

## 一、功能概述

实现"导入团队库组件"功能，允许用户通过左侧tab"组件库"进入，浏览"组件库"项目中的组件，导入到当前文件中。

### 功能定位

- **主功能**：浏览"组件库"项目中的组件并导入
- **操作流程**：点击左侧tab"组件库" → 显示文件列表（单文件自动进入） → 显示页面列表（单页面自动进入） → 显示组件列表 → 导入组件

---

## 二、核心发现

根据 MasterGo 团队库 API 文档，发现以下关键 API：

### 1. 团队库 API（已验证）

- **`getTeamLibraryAsync()`** - 获取已订阅的团队库组件和样式信息
  - 返回类型：`Promise<TeamLibrary>`
  - `TeamLibrary` 是 `ReadonlyArray<{ name, id, componentList, style }>`
  - 每个团队库包含 `name`、`id` 和 `componentList`（组件列表）
  
- **`importComponentByKeyAsync(ukey: string)`** - 通过 ukey 从团队库导入组件到画布
  - 返回类型：`Promise<ComponentNode>`
  - 可以创建实例使用

### 2. 数据结构

根据 API 文档，`TeamLibrary` 的结构如下：

```typescript
type TeamLibrary = ReadonlyArray<{
  readonly name: string;        // 团队库名称
  readonly id: string;          // 团队库ID
  readonly componentList: TeamLibraryComponent[];  // 组件列表
  readonly style: { ... };      // 样式信息
}>
```

`TeamLibraryComponent` 的结构（已验证）：

```typescript
interface TeamLibraryComponent {
  readonly id: string;
  readonly name: string;
  readonly ukey: string;        // 用于导入的 ukey
  readonly description: string;
  readonly type: "COMPONENT" | "COMPONENT_SET";
  readonly cover: string;
  readonly width: number;
  readonly height: number;
}
```

⚠️ **重要发现**：
- `TeamLibraryComponent` **不包含项目信息**
- 无法通过组件数据直接筛选出"组件库"项目的组件
- 需要采用替代方案来识别或筛选组件

### 3. 重要发现

⚠️ **层级 API 不存在**：
- 无法获取团队列表
- 无法获取项目列表
- 无法获取文件列表
- 无法获取页面列表
- **只能通过 `getTeamLibraryAsync()` 获取已订阅的团队库及其组件列表**

⚠️ **组件数据结构限制**：
- `TeamLibraryComponent` **不包含项目信息**
- 无法通过组件数据直接识别组件来自哪个项目
- 需要采用替代方案来筛选"组件库"项目的组件

### 2. API 使用示例

```typescript
async function importComponentFromLibrary(ukey: string) {
  try {
    const componentNode = await mg.importComponentByKeyAsync(ukey);
    const instance = componentNode.createInstance();
    // 其他操作
  } catch (error) {
    // 当传入的 ukey 无法关联到团队库组件时会抛错
    console.error('failed to import component', error);
  }
}
```

---

## 三、功能设计

### 3.1 功能模块

#### 浏览团队库组件并导入
- **输入**：无（自动获取）
- **处理**：
  1. 调用 `getTeamLibraryAsync()` 获取所有已订阅的团队库
  2. 遍历所有团队库的 `componentList`，收集全部组件
  3. 在 UI 中显示所有组件，支持搜索/筛选（例如按名称、按类型）
  4. 用户选择组件后，调用 `importComponentByKeyAsync(ukey)` 导入
- **输出**：组件实例添加到当前画布
- **注意**：由于 `TeamLibraryComponent` 不包含项目信息，无法精确区分组件所属项目，因此显示全部组件，由用户手动选择

### 3.2 数据流设计

```
点击左侧tab"组件库"
  → 调用 getTeamLibraryAsync() 获取所有已订阅的团队库
    → 遍历所有团队库，收集所有组件
    → 显示组件列表（支持搜索/筛选）
      → 用户选择组件
        → 使用组件的 ukey
          → 调用 importComponentByKeyAsync(ukey)
            → 导入组件至当前文件
```

### 3.3 数据结构

```
TeamLibrary (数组) - 所有已订阅的团队库
  └─ TeamLibraryItem
      ├─ name: string（团队库名称）
      ├─ id: string
      └─ componentList: TeamLibraryComponent[]
          └─ TeamLibraryComponent
              ├─ name: string（组件名称）
              ├─ ukey: string（用于导入）
              ├─ type: "COMPONENT" | "COMPONENT_SET"
              ├─ description: string
              ├─ cover: string
              ├─ width: number
              ├─ height: number
              └─ ...（可能包含项目信息，需要验证）
```

### 3.4 关键问题与解决方案

⚠️ **项目信息缺失**
- `TeamLibraryComponent` **不包含项目名称信息**（已确认）
- 无法通过组件数据直接识别组件来自哪个项目

✅ **采用的解决方案**
- 显示所有已订阅团队库中的组件，让用户自行选择
- 在 UI 中提供搜索、筛选功能，帮助用户快速定位目标组件
- 可在组件描述中提示所属项目（如果遵循命名规范）
- 如果后续 MasterGo API 提供项目信息，再考虑精确筛选

---

## 四、技术实现方案

### 4.1 消息类型定义

在 `Variant/src/messages/messageType.ts` 中添加：

```typescript
// 组件导入相关
IMPORT_COMPONENT_BY_UKEY = 'importComponentByUkey',      // 通过 ukey 导入组件（内部使用）
GET_COMPONENT_LIBRARY = 'getComponentLibrary',            // 获取"组件库"团队库的组件列表
```

### 4.2 数据格式设计

```typescript
// 通过 ukey 导入（内部使用，用户无需输入）
interface ImportByUkeyData {
  ukey: string;  // 组件的 ukey（从组件列表中选择）
}

// 组件信息（基于 TeamLibraryComponent）
interface ComponentInfo {
  id: string;
  name: string;
  ukey: string;              // 用于导入的 ukey
  description: string;
  type: "COMPONENT" | "COMPONENT_SET";
  cover: string;             // 封面图片URL
  width: number;
  height: number;
}

// 团队库组件列表响应
interface ComponentLibraryResponse {
  components: ComponentInfo[];
  libraryName: string;      // 团队库名称
  libraryId: string;        // 团队库ID
}
```

### 4.3 核心处理逻辑

#### 方式1：直接通过 ukey 导入

```typescript
/**
 * 通过 ukey 导入团队库组件
 * @param ukey 组件的唯一标识符
 */
async function importComponentByUkey(ukey: string) {
  const currentPage = (mg as any).document?.currentPage;
  if (!currentPage) {
    mg.notify('当前页面不可用', { timeout: 2000 });
    return;
  }

  try {
    // 使用 MasterGo API 导入组件
    const componentNode = await mg.importComponentByKeyAsync(ukey);
    
    // 创建实例
    const instance = componentNode.createInstance();
    
    // 添加到当前页面（定位到视口中心）
    const viewportCenter = (mg as any).viewport?.center || { x: 0, y: 0 };
    instance.x = viewportCenter.x - instance.width / 2;
    instance.y = viewportCenter.y - instance.height / 2;
    currentPage.appendChild(instance);
    
    mg.notify('组件导入成功', { timeout: 2000 });
  } catch (error: any) {
    console.error('导入组件失败:', error);
    mg.notify('导入失败：ukey 无效或组件不在团队库中', { timeout: 3000 });
  }
}
```

#### 方式2：获取所有团队库组件（并由用户筛选）

```typescript
/**
 * 获取所有已订阅团队库中的组件
 * @returns 组件列表
 */
async function getTeamLibraryComponents(): Promise<ComponentLibraryResponse | null> {
  try {
    const teamLibraries = await mg.getTeamLibraryAsync();
    
    if (!teamLibraries || teamLibraries.length === 0) {
      mg.notify('未订阅任何团队库', { timeout: 2000 });
      return null;
    }
    
    // 收集所有团队库中的组件
    const allComponents: ComponentInfo[] = [];
    teamLibraries.forEach((lib: any) => {
      if (lib.componentList && lib.componentList.length > 0) {
        lib.componentList.forEach((comp: any) => {
          allComponents.push({
            id: comp.id || '',
            name: comp.name || '未命名组件',
            ukey: comp.ukey || '',
            description: comp.description || '',
            type: comp.type || 'COMPONENT',
            cover: comp.cover || '',
            width: comp.width || 0,
            height: comp.height || 0,
            libraryName: lib.name,  // 记录来源团队库，便于用户识别
          });
        });
      }
    });
    
    if (allComponents.length === 0) {
      mg.notify('团队库中暂无组件', { timeout: 2000 });
      return null;
    }
    
    return {
      components: allComponents,
      libraryName: '已订阅团队库',
      libraryId: '',
    };
  } catch (error: any) {
    console.error('获取组件列表失败:', error);
    mg.notify('获取组件列表失败', { timeout: 2000 });
    return null;
  }
}
```

---

## 五、UI 设计方案

### 5.1 主界面布局

在 `Variant/src/ui/App.vue` 中添加左侧tab"组件库"，创建对应的页面组件。

#### 界面结构

```
┌─────────────────────────────────────────┐
│  左侧导航栏                              │
│  [资源位]                                │
│  [H5切图]                                │
│  [H5延展]                                │
│  [工具箱]                                │
│  [组件库] ← 新增tab                      │
├─────────────────────────────────────────┤
│  右侧内容区                              │
│                                         │
│  组件库                                  │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │ [封面] 按钮组件                    │ │
│  │ 描述：通用按钮组件                │ │
│  │                          [导入]   │ │
│  └───────────────────────────────────┘ │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │ [封面] 卡片组件                    │ │
│  │ 描述：信息展示卡片                │ │
│  │                          [导入]   │ │
│  └───────────────────────────────────┘ │
│                                         │
│  ...                                     │
│                                         │
└─────────────────────────────────────────┘
```

### 5.2 组件库页面界面

#### 界面元素

- **标题区域**
  - 显示"组件库"（项目名称）
  - 显示组件总数（可选）

- **组件列表视图**
  - 显示当前已订阅团队库中的所有组件（可通过搜索/筛选定位"组件库"项目组件）
  - 每个组件卡片显示：
    - 组件封面图片（如果有 `cover`）
    - 组件名称
    - 组件描述（如果有）
    - 组件类型标识（COMPONENT 或 COMPONENT_SET）
    - 组件尺寸（width × height，可选）
    - 来源团队库名称（可选，如果组件来自多个团队库）
    - "导入"按钮（主要操作）
  - 支持滚动浏览
  - 支持搜索/筛选功能（可选）

- **加载状态**
  - 获取数据时显示加载动画
  - 如果未订阅任何团队库，显示提示："未订阅任何团队库，请在 MasterGo 中订阅团队库"
  - 如果组件列表为空，显示提示："暂无可用组件，请检查团队库"

- **搜索功能**（可选）
  - 支持按组件名称搜索
  - 支持按组件类型筛选（组件/组件集）

#### 交互流程

1. **点击左侧tab"组件库"**：切换到组件库页面
2. **自动获取组件列表**：系统自动调用 `getTeamLibraryAsync()`，收集所有已订阅团队库中的组件
3. **显示组件列表**：展示所有组件，并提供搜索/筛选功能供用户快速定位
4. **组件选择**：用户浏览组件列表，找到需要的组件
5. **导入组件**：用户点击组件的"导入"按钮
6. **完成**：系统使用组件的 ukey 调用 `importComponentByKeyAsync(ukey)`，组件添加到当前画布中心，显示成功/失败提示

### 5.4 样式设计

- 使用项目统一的样式变量
- 保持与现有页面（batch-extend、batch-button）一致的风格
- 响应式布局，适配不同窗口大小

---

## 六、文件结构

```
Variant/
├── src/
│   ├── messages/
│   │   └── messageType.ts                    # 添加2个新消息类型
│   └── ui/
│       ├── App.vue                           # 修改：添加"组件库"tab
│       └── pages/
│           └── component-library/
│               └── index.vue                 # 新建：组件库页面
└── mastergo/
    └── src/
        └── plugin/
            ├── index.ts                      # 注册新消息处理器
            └── messages/
                └── toolbox/
                    ├── import-component-by-ukey.ts          # 新建：通过 ukey 导入（内部使用）
                    └── get-component-library.ts            # 新建：获取"组件库"团队库组件列表
```

### 文件说明

| 文件路径 | 说明 | 行数限制 |
|---------|------|---------|
| `messageType.ts` | 添加2个新消息类型 | - |
| `App.vue` | 添加左侧tab"组件库" | - |
| `component-library/index.vue` | 组件库页面（组件列表展示） | ≤200 |
| `import-component-by-ukey.ts` | 通过 ukey 导入的处理逻辑（内部使用） | ≤200 |
| `get-component-library.ts` | 获取已订阅团队库组件列表的处理逻辑 | ≤200 |
| `index.ts` | 插件主入口，注册消息处理器 | - |

---

## 七、实现优先级

### 阶段1：核心功能（必须实现）⭐

1. ✅ **左侧tab"组件库"**
   - 在 App.vue 中添加"组件库"tab
   - 创建组件库页面组件

2. ✅ **获取团队库组件列表**
   - 实现 `getComponentLibrary` 消息处理器
   - 使用 `getTeamLibraryAsync()` 获取所有已订阅的团队库
   - 收集所有团队库的组件列表
   - 返回统一的组件列表，供 UI 展示

3. ✅ **组件列表展示**
   - 实现组件列表视图
   - 显示组件封面、名称、描述等信息
   - 支持滚动浏览
   - 每个组件显示"导入"按钮

4. ✅ **导入组件功能**
   - 实现 `importComponentByUkey` 消息处理器（内部使用）
   - 使用 `importComponentByKeyAsync(ukey)` API
   - 将组件添加到当前画布中心

### 阶段2：用户体验优化（后续）✨

1. **ukey 复制功能**
   - 一键复制 ukey 到剪贴板

2. **组件搜索功能**
   - 在团队库浏览器中搜索组件

3. **导入历史记录**
   - 记录最近导入的组件
   - 快速重新导入

4. **组件预览**
   - 导入前预览组件样式

---

## 八、错误处理

### 错误场景及处理

| 场景 | 错误原因 | 处理方式 | 用户提示 |
|------|---------|---------|---------|
| ukey 无效 | ukey 格式错误或不存在 | 捕获异常，显示错误提示 | "ukey 无效，请检查是否正确" |
| 组件不在团队库 | 组件未订阅或已删除 | 捕获异常，显示错误提示 | "该组件不在已订阅的团队库中" |
| 未订阅任何团队库 | 没有订阅任何团队库 | 检查团队库列表长度，显示提示 | "未订阅任何团队库，请在 MasterGo 中订阅团队库" |
| 组件列表为空 | 团队库中没有组件 | 检查组件列表长度，显示提示 | "暂无可用组件，请检查团队库发布状态" |
| ukey 无效 | 组件的 ukey 无效或组件已删除 | 捕获异常，显示提示 | "导入失败：ukey 无效或组件不在团队库中" |
| API 调用失败 | 网络或权限问题 | 捕获异常，显示详细错误 | "获取团队库失败：{错误信息}" |
| 当前页面不可用 | 文档未打开 | 检查页面状态，显示提示 | "当前页面不可用，请先打开文档" |

### 错误处理代码示例

```typescript
try {
  // API 调用
  const result = await mg.importComponentByKeyAsync(ukey);
  // 成功处理
} catch (error: any) {
  // 错误处理
  console.error('导入组件失败:', error);
  
  // 根据错误类型显示不同提示
  if (error.message?.includes('ukey')) {
    mg.notify('ukey 无效，请检查是否正确', { timeout: 3000 });
  } else if (error.message?.includes('团队库')) {
    mg.notify('该组件不在已订阅的团队库中', { timeout: 3000 });
  } else {
    mg.notify('导入失败：' + error.message, { timeout: 3000 });
  }
}
```

---

## 九、用户操作指引

### 9.1 导入组件步骤

#### 操作流程

1. **点击左侧tab"组件库"**：切换到组件库页面
2. **自动获取组件列表**：系统自动调用 `getTeamLibraryAsync()`，获取所有已订阅的团队库，并收集全部组件
3. **显示组件列表**：将所有组件统一展示，可通过搜索/筛选快速定位
5. **浏览组件**：用户浏览组件列表，可以查看组件封面、名称、描述等信息
6. **选择组件**：用户找到需要的组件，点击"导入"按钮
7. **导入组件**：系统使用组件的 ukey 调用 `importComponentByKeyAsync(ukey)`
8. **完成**：组件将自动添加到当前画布中心，显示导入成功/失败提示

#### 注意事项

- 功能入口在左侧tab"组件库"，不在工具箱中
- **"组件库"是项目名称**，不是团队库名称
- 由于 `TeamLibraryComponent` 不包含项目信息，系统会展示所有已订阅团队库的组件
- 用户可以通过搜索关键字（如组件命名规范）来定位"组件库"项目的组件
- 支持搜索/筛选功能（可选，便于在大量组件中快速查找）
- 如果导入失败，会显示具体错误信息
- 组件会添加到当前视口中心位置
- **重要**：导入的团队库组件是不可见的，只能通过创建实例来使用

---

## 十、实现步骤

### 步骤1：添加消息类型

**文件**：`Variant/src/messages/messageType.ts`

```typescript
// 组件导入相关
IMPORT_COMPONENT_BY_UKEY = 'importComponentByUkey',  // 内部使用，用户选择组件后自动调用
GET_COMPONENT_LIBRARY = 'getComponentLibrary',       // 获取"组件库"团队库的组件列表
```

### 步骤2：创建消息处理器

**文件1**：`Variant/mastergo/src/plugin/messages/toolbox/get-component-library.ts`

- 实现获取"组件库"项目组件列表的逻辑
- 使用 `mg.getTeamLibraryAsync()` 获取所有已订阅的团队库
- 遍历所有团队库的组件列表
- 尝试筛选出来自"组件库"项目的组件（需要验证组件数据结构是否包含项目信息）
- 如果无法区分项目，返回所有组件或提示用户
- 格式化返回数据（包含组件信息：id, name, ukey, description, type, cover, width, height）

**文件2**：`Variant/mastergo/src/plugin/messages/toolbox/import-component-by-ukey.ts`

- 实现通过 ukey 导入组件的逻辑（内部使用）
- 使用 `mg.importComponentByKeyAsync(ukey)`
- 创建组件实例
- 将组件添加到当前画布中心
- 处理错误和异常情况

### 步骤3：创建 UI 页面

**文件1**：`Variant/src/ui/pages/component-library/index.vue`（新建）

- 创建组件库页面
- 页面加载时自动获取组件列表
- 实现组件列表视图
- 显示组件卡片（封面、名称、描述、类型、尺寸等）
- 每个组件显示"导入"按钮
- 点击"导入"按钮后，发送 ukey 到插件进行导入
- 实现消息发送和接收
- 显示加载状态和错误提示
- 支持搜索/筛选功能（可选）

**文件2**：`Variant/src/ui/App.vue`（修改）

- 在左侧导航栏添加"组件库"tab
- 添加组件库页面的路由配置
- 实现tab切换逻辑

### 步骤4：集成到工具箱

**文件**：`Variant/src/ui/pages/toolbox/index.vue`

- 在"实用工具"区域添加"导入组件"按钮
- 添加视图状态管理
- 添加页面切换逻辑

### 步骤4：注册消息处理器

**文件**：`Variant/mastergo/src/plugin/index.ts`

- 导入新的消息处理器
- 添加到 messages 数组
- 确保消息路由正确

### 步骤5：测试和调试

- 测试左侧tab"组件库"切换功能
- 测试获取"组件库"团队库组件列表功能
- 测试组件列表展示（封面、名称、描述等）
- 测试组件选择导入功能
- 测试错误处理（未找到团队库、组件列表为空、导入失败等）
- 测试 UI 交互（加载状态、按钮点击、滚动浏览等）
- 测试搜索/筛选功能（如果实现）

### 步骤6：记录日志

**文件**：`logs/2025-11-21.md`

- 记录实现的功能
- 记录遇到的问题和解决方案
- 记录后续优化方向

---

## 十一、方案优势

### 11.1 技术优势

✅ **基于官方 API**
- 使用 MasterGo 官方提供的团队库 API
- 稳定可靠，符合平台规范

✅ **功能完整**
- 提供多种导入方式
- 支持团队库浏览
- 用户体验良好

✅ **错误处理完善**
- 覆盖各种错误场景
- 提供清晰的错误提示
- 提供操作指引

### 11.2 用户体验优势

✅ **操作简单**
- 一键浏览和导入
- 无需手动输入 ukey
- 直观的组件列表展示

✅ **界面友好**
- 简洁的列表展示
- 清晰的操作按钮
- 即时的反馈提示

✅ **功能聚焦**
- 专注于团队库浏览和导入
- 操作流程简单直接

### 11.3 开发优势

✅ **分阶段实现**
- 核心功能优先
- 增强功能可选
- 便于迭代优化

✅ **代码规范**
- 遵循项目规范
- 文件行数控制
- 中文注释完善

✅ **易于维护**
- 模块化设计
- 清晰的代码结构
- 完善的错误处理

---

## 十二、注意事项

### 12.1 团队库要求

⚠️ **项目要求**
- "组件库"是项目名称，不是团队库名称
- 需要确保"组件库"项目中的组件已发布到团队库
- 需要订阅包含"组件库"项目组件的团队库
- 如果订阅了多个团队库，组件列表会显示所有组件，用户需自行辨识
- 可通过命名规范、描述等方式标记"组件库"项目组件，便于搜索

⚠️ **数据结构限制**
- `TeamLibraryComponent` **不包含项目名称信息**（已确认）
- 无法通过组件数据直接识别组件来自哪个项目
- **解决方案**：展示所有组件，配合搜索/筛选，或依赖命名规范

⚠️ **API 限制**
- 根据 API 文档，层级 API（团队、项目、文件、页面）不存在
- 只能通过 `getTeamLibraryAsync()` 获取已订阅的团队库及其组件列表
- 无法获取文件/页面的层级结构
- 组件列表直接来自团队库，不区分文件/页面来源

⚠️ **导入组件限制**
- 导入的团队库组件是不可见的，数据存储在远端
- 只能通过 `createInstance()` 创建实例来使用
- 无法修改导入组件的其他属性或调用其他方法
- 这是 MasterGo API 的设计限制

---

## 十六、可行性分析

### 16.1 API 验证状态

根据查询 MasterGo 官方文档，API 验证状态如下：

#### ✅ 已验证的 API（可直接使用）

1. **`getTeamLibraryAsync()`** - ✅ 已确认存在
   - 功能：获取当前已订阅的团队库组件和样式信息
   - 状态：可直接使用

2. **`importComponentByKeyAsync(ukey: string)`** - ✅ 已确认存在
   - 功能：从已订阅的团队库中导入组件到画布中
   - 状态：可直接使用

#### ⚠️ 需要验证的 API（方案中假设存在）

1. **`getCurrentTeamAsync()` 或 `mg.document.team`** - ⚠️ 需要验证
2. **`getTeamProjectsAsync(teamId: string)`** - ⚠️ 需要验证
3. **`getProjectFilesAsync(projectId: string)`** - ⚠️ 需要验证
4. **`getFilePagesAsync(fileId: string)`** - ⚠️ 需要验证
5. **`getPageComponentsAsync(pageId: string)`** - ⚠️ 需要验证

### 16.2 可行性评估

| 功能模块 | API 状态 | 可行性 | 备注 |
|---------|---------|--------|------|
| 导入组件 | ✅ 已验证 | ✅ 可行 | 使用 `importComponentByKeyAsync(ukey)` |
| 获取组件列表 | ✅ 已验证 | ✅ 可行 | 使用 `getTeamLibraryAsync()` |
| 层级导航 | ⚠️ 待验证 | ⚠️ 需验证 | 取决于层级 API 是否支持 |

### 16.3 推荐实现策略

#### 方案A：完整层级导航（如果 API 支持）

- 实现完整的层级导航：团队 → 项目 → 文件 → 页面 → 组件
- 优点：用户体验好，结构清晰
- 缺点：需要验证多个 API

#### 方案B：简化版本（推荐优先实现）

如果层级 API 不支持，使用以下简化方案：

1. **使用 `getTeamLibraryAsync()` 直接获取所有组件**
2. **在 UI 中显示组件列表**
3. **支持搜索/筛选功能**
4. **用户选择后导入**

**优点**：
- ✅ 使用已验证的 API，风险低
- ✅ 实现简单，开发周期短
- ✅ 功能可用，满足基本需求

**缺点**：
- ❌ 无法按文件/页面组织组件
- ❌ 如果组件很多，列表可能很长

### 16.4 详细分析文档

详细的可行性分析请参考：**`组件库功能-可行性分析.md`**

该文档包含：
- 已验证的 API 列表
- 需要验证的 API 列表
- 替代方案
- API 验证步骤
- 开发建议

### 12.2 权限要求

⚠️ **文件权限**
- 用户需要有访问团队库的权限
- 某些组件可能需要特定权限才能导入

### 12.3 性能考虑

⚠️ **团队库大小**
- 如果团队库组件很多，加载可能较慢
- 考虑添加分页或搜索功能
- 考虑缓存团队库数据

---

## 十三、后续优化方向

### 13.1 功能增强

- [ ] 组件搜索功能
- [ ] 组件分类筛选
- [ ] 导入历史记录
- [ ] 组件预览功能
- [ ] 批量导入功能

### 13.2 用户体验

- [ ] 快捷键支持
- [ ] 拖拽导入
- [ ] 导入位置记忆
- [ ] 组件使用统计

### 13.3 技术优化

- [ ] 团队库数据缓存
- [ ] 异步加载优化
- [ ] 错误重试机制
- [ ] 性能监控

---

## 十四、参考资料

- [MasterGo 插件开发文档](https://developers.mastergo.com/)
- [MasterGo 团队库 API](https://developers.mastergo.com/guide/team-library.html)
- 项目现有代码结构
- 项目编码规范

---

## 十五、版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2025-11-21 | 初始方案，基于团队库 API 设计 |
| v1.1 | 2025-11-21 | 移除文件链接解析功能，简化为 ukey 导入和团队库浏览 |
| v1.2 | 2025-11-21 | 简化操作流程，移除 ukey 输入，仅保留团队库浏览器 |
| v1.3 | 2025-11-21 | 固定浏览名为"组件库"的团队库项目 |
| v2.0 | 2025-11-21 | 重构为层级导航结构：团队 → 项目 → 文件 → 页面 → 组件 |
| v2.1 | 2025-11-21 | 简化操作流程：左侧tab"组件库"，单文件/单页面自动进入 |
| v2.2 | 2025-11-21 | 添加可行性分析，明确已验证和待验证的 API |
| v3.0 | 2025-11-21 | 根据实际 API 文档优化方案，移除层级导航，直接使用 getTeamLibraryAsync() 获取组件列表 |
| v3.1 | 2025-11-21 | 修正理解："组件库"是项目名称而非团队库名称，需要筛选来自该项目的组件 |
| v3.2 | 2025-11-21 | 确认 TeamLibraryComponent 不包含项目信息，采用通过团队库名称筛选的方案 |

---

## 十六、方案优化说明（v3.0）

### 16.1 优化原因

根据 MasterGo 官方 API 文档验证，发现：
- ✅ `getTeamLibraryAsync()` 和 `importComponentByKeyAsync(ukey)` 已确认存在
- ❌ 层级 API（团队、项目、文件、页面）不存在

### 16.2 优化内容

1. **移除层级导航**
   - 不再需要获取团队、项目、文件、页面列表
   - 直接使用 `getTeamLibraryAsync()` 获取组件列表

2. **简化数据流**
   - 从：团队 → 项目 → 文件 → 页面 → 组件
   - 到：团队库 → 组件列表

3. **简化消息类型**
   - 从 4 个消息类型减少到 2 个
   - 只保留：`GET_COMPONENT_LIBRARY` 和 `IMPORT_COMPONENT_BY_UKEY`

4. **简化文件结构**
   - 从 4 个消息处理器减少到 2 个
   - 只保留：`get-component-library.ts` 和 `import-component-by-ukey.ts`

### 16.3 数据结构说明

根据 API 文档，`getTeamLibraryAsync()` 返回的数据结构：

```typescript
type TeamLibrary = ReadonlyArray<{
  readonly name: string;        // 团队库名称
  readonly id: string;          // 团队库ID
  readonly componentList: TeamLibraryComponent[];  // 组件列表
  readonly style: { ... };      // 样式信息（本功能不使用）
}>
```

`TeamLibraryComponent` 包含：
- `id`: 组件ID
- `name`: 组件名称
- `ukey`: 用于导入的 ukey（关键字段）
- `description`: 组件描述
- `type`: "COMPONENT" | "COMPONENT_SET"
- `cover`: 封面图片URL
- `width`: 组件宽度
- `height`: 组件高度
- **不包含项目信息**（确认）

⚠️ **关键问题**：
- 无法通过组件数据直接区分项目
- 需要在 UI 中提供搜索/筛选能力，或依赖命名规范

### 16.4 实现要点

1. **获取所有团队库组件**
   ```typescript
   const teamLibraries = await mg.getTeamLibraryAsync();
   const components = [];
   teamLibraries.forEach(lib => {
     lib.componentList?.forEach(comp => {
       components.push({
         ...comp,
         libraryName: lib.name,
         libraryId: lib.id,
       });
     });
   });
   ```

2. **展示组件列表**
   - 将所有组件统一展示
   - 在 UI 中提供搜索、筛选功能（按名称、类型、来源团队库等）
   - 引导用户通过命名规范或描述来识别“组件库”项目组件

3. **导入组件**
   ```typescript
   const componentNode = await mg.importComponentByKeyAsync(ukey);
   const instance = componentNode.createInstance();
   ```

### 16.5 筛选策略说明

由于 `TeamLibraryComponent` 不包含项目信息，策略如下：

- **统一展示**：显示所有已订阅团队库中的组件
- **搜索/筛选**：提供按名称、类型、来源团队库的筛选能力
- **命名规范**：建议“组件库”项目的组件采用统一命名，便于搜索
- **后续优化**：如果 MasterGo 提供项目信息，再实现精确筛选

### 16.5 优势

- ✅ 使用已验证的 API，风险低
- ✅ 实现简单，开发周期短
- ✅ 代码量少，易于维护
- ✅ 功能可用，满足基本需求

---

**文档创建时间**：2025-11-21  
**最后更新时间**：2025-11-21  
**文档状态**：已优化，基于实际 API 文档
